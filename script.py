# Automating terminal command
# Excuting Shell command using code.
import subprocess
for i in range(5):
    # The list contains the arguments of our comman line code.
    subprocess.check_call(['python', 'example.py'])